export const CalendarEventBox = ({ event }) => {

    const { title, user } = event

  return (
    <>
        <div style={{textAlign: 'center'}}>
            <strong> {title} </strong>
        </div>
        

    </>
  );

};
